{
  "name": "agent-codemod",
  "version": "1.0.0",
  "description": "Automated codemod and migration agent",
  "outputDir": ".claude",
  "files": ["agents/codemod.md"],
  "postMessage": "Codemod agent installed! Give it a migration task — e.g., 'migrate all useState callbacks to useReducer' or 'upgrade React Router v5 to v6'."
}
