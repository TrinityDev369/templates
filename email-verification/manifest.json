{
  "name": "email-verification",
  "version": "1.0.0",
  "description": "Email verification transactional template with confirmation button, fallback URL, and expiration notice. Built with React Email.",
  "outputDir": "emails",
  "files": ["verification.tsx", "index.ts"],
  "peerDependencies": {
    "@react-email/components": ">=0.0.20",
    "react": ">=18"
  },
  "postMessage": "Verification email installed! Preview: npx email dev\nImport: import { VerificationEmail } from '@/emails/verification'"
}
