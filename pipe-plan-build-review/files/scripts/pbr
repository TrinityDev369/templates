#!/bin/bash
# PBR Pipeline -- Plan, Build, Review, Fix
# Thin wrapper that delegates to the pipeline orchestrator.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/../pipeline/pbr.sh" "$@"
